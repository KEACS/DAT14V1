public class Exercise2MixingNodes2
{
   
   public static void main(String[] args)
   {
      ListNode dummy = new ListNode(1);
      
      ListNode list = new ListNode(6, new ListNode(3));
      ListNode temp = new ListNode(5, new ListNode(2));
      ListNode temp2 = new ListNode(4, new ListNode(1));
      
      // Order these nodes in a single list
      // Follow the same principle
      
      // extra: 
      // ListNode list = new ListNode(6, new ListNode(3, new ListNode(2, new ListNode(1))));
   }
}